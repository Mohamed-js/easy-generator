{
  "name": "easy-generator-test",
  "version": "1.0.0",
  "description": "a test project for interview",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install": "npm install && npm run install-b && npm run install-f",
    "install-f": "cd ./frontend && npm install",
    "install-b": "cd ./backend && npm install",
    "dev": "concurrently \"npm run dev-b\" \"npm run dev-f\"",
    "dev-f": "cd ./frontend && npm run dev",
    "dev-b": "cd ./backend && npm run dev"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}